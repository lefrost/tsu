tasks:
  build:
    script: 'bun run --env-file=../../.env build ; bun run --env-file=../../.env preview'
  dev:
    script: 'bun run --env-file=../../.env dev'
  install:
    script: 'rm -rf node_modules ; pnpm install'
  update:
    script: 'rm -rf node_modules ; pnpm up --latest'