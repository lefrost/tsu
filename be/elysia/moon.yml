tasks:
  build:
    script: 'bun run --env-file=../../.env dev'
  dev:
    script: 'bun run --env-file=../../.env dev'
  install:
    script: 'rm -rf node_modules ; bun install' # use bun over pnpm for elysia installation specifically