tasks:
  build:
    script: 'bun run --env-file=../../.env dev'
  dev:
    script: 'bun run --env-file=../../.env dev'
  install:
    script: 'bun run --env-file=../../.env install' # use bun over pnpm for elysia installation specifically