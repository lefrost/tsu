id: "elysia"

tasks:
  build:
    script: 'bun run --env-file=../../.env dev'
  dev:
    script: 'bun run --env-file=../../.env dev'
  install:
    script: 'bun install --force' # use bun over pnpm for elysia installation specifically
  update:
    script: 'bun update --latest'